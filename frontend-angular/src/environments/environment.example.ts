// Copy to environment.ts (`cp src/environments/environment.example.ts
// src/environments/environment.ts`) and edit for local `ng serve`. See
// environment.ts for why this isn't a `.env` file.
export const environment = {
  apiBaseUrl: 'http://localhost:8080/api',
};
